package main

import "fmt"

// Definition for singly-linked list.
type ListNode struct {
	Val  int
	Next *ListNode
}

 func removeElements(head *ListNode, val int) *ListNode {
    // walk thru linked list
    for ptr1, ptr2 := head, head.next; ptr1, ptr2 !=nil {
		
    }

    // for each element if it is == val then drop it

    // but then you can't just drop it - need to remove and point to next element
}

func main() {
	fmt.Println("I ran")
	input := 
}
